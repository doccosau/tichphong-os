/**
 * Music Driver — I/O Layer
 * Constitution §6: Driver CHỈ làm I/O (Network/Hardware). KHÔNG chứa logic nghiệp vụ.
 *
 * TODO (Phase 2): Migrate API calls từ services/ vào đây.
 * Hiện tại: Stub file đánh dấu cấu trúc chuẩn.
 */

export class MusicDriver {
    static readonly SUBSYSTEM = 'music';

    /**
     * Placeholder — sẽ chứa Worker API calls (songs, playlists, search)
     */
}
