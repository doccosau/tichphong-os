/**
 * Music Kernel — Business Logic
 * Constitution §6: Kernel chứa logic nghiệp vụ, KHÔNG gọi UI hoặc API bên ngoài.
 *
 * TODO (Phase 2): Migrate logic từ LibraryStore vào đây.
 * Hiện tại: Stub file đánh dấu cấu trúc chuẩn.
 */

export class MusicKernel {
    static readonly SUBSYSTEM = 'music';

    /**
     * Placeholder — sẽ chứa logic playlist, queue management, search
     */
}
