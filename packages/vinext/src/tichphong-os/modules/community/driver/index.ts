/**
 * Community Driver — I/O Layer (RealtimeDriver WebSocket)
 * Constitution §6: Driver CHỈ làm I/O.
 * TODO (Phase 2): Migrate RealtimeDriver calls vào đây.
 */
export class CommunityDriver {
    static readonly SUBSYSTEM = 'community';
}
