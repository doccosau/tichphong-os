/**
 * Community Store — L1 UI Cache
 * Constitution §6: Store CHỈ dùng để hiển thị dữ liệu lên UI.
 * TODO (Phase 2): Migrate room/chat state vào đây.
 */
export class CommunityStore {
    static readonly SUBSYSTEM = 'community';
}
